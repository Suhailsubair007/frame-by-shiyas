export const LAYOUT = {
  // Fixed-header height in px. Section scroll targets subtract this so a heading
  // isn't hidden behind TheNavigation when reached via an in-page nav link.
  HEADER_OFFSET: 80,
} as const
